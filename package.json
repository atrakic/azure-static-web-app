{
  "directories": {
    "test": "tests"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.6",
    "@playwright/test": "^1.54.2",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "start": "swa start app --api-location api",
    "format": "prettier --no-error-on-unmatched-pattern --write 'api/**/*.js' *.md'' 'docs/**/*.md' 'schema/**/*.json'",
    "test": "playwright test"
  }
}
